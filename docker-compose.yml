services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: fervor
      POSTGRES_PASSWORD: fervor
      POSTGRES_DB: fervor
    volumes:
      - fervor_pgdata:/var/lib/postgresql/data

volumes:
  fervor_pgdata:
